import { ProjectsSection } from "portfolio";

// Self-contained — reads its project list from config/projects.ts and
// translations directly, same as the real page (app/[locale]/page.tsx).
export const Default = () => <ProjectsSection />;
