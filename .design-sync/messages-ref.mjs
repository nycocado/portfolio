// Real i18n messages, referenced (not copied) so previews always match the
// live copy — wired via cfg.provider's {"$ref": "enMessages"}.
import enMessages from "../messages/en.json";
export { enMessages };
